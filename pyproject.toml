[project]
name = "tara-stt"
version = "1.0.0"
description = "Simple Tara faster-whisper FastAPI STT"
requires-python = ">=3.12"
dependencies = [
    "faster-whisper>=1.1.0",
    "ctranslate2>=4.4.0",
    "fastapi",
    "uvicorn[standard]",
    "python-multipart",
    "silero-vad",
    "numpy",
    "torch",
]
